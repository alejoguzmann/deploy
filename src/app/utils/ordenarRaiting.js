export const orderRating = (cards = []) => {
//  const reviewedCards = cards.filter(card => card?.reviews?.length > 0)
 const reviewedCards = cards
    // return cards.sort((a, b) => {
    //   const reviewA = a.reviews.rating;
    //   const reviewB = b.reviews.rating;
  
    //   return reviewB - reviewA;
    // });
  };

  